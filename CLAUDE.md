# CLAUDE.md — WellSpent-iOS

Native iOS (SwiftUI) client for WellSpent. Consumes the same ConnectRPC API as `WellSpent-web`, via the same `wellspent.v1` proto contracts published by `WellSpent-proto`.

`WellSpent.xcodeproj` lives directly at the repo root, alongside `WellSpent/` (app sources), `WellSpentTests/`, `WellSpentUITests/`, `Packages/WellSpentAPI/`, and `buf.gen.yaml` — same flat layout as the other three sub-repos. (Xcode's project-creation dialog defaults to nesting everything one level deeper, inside a `<ProductName>/` subfolder; that extra nesting was flattened out during initial setup.)

## Commands

Run all of these from the repo root (`WellSpent-iOS/`):

```bash
make generate     # buf generate — pulls proto from BSR, generates Swift into Packages/WellSpentAPI/Sources/WellSpentAPI/Gen/
swift build --package-path Packages/WellSpentAPI   # fast standalone compile check of just the generated + client layer
swift test --package-path Packages/WellSpentAPI    # WellSpentAPI's own unit tests (AuthInterceptor etc.)

xcodebuild -list -project WellSpent.xcodeproj                                          # confirm scheme name
xcrun simctl list devices available                                                    # pick an installed simulator
xcodebuild -scheme WellSpent -destination 'platform=iOS Simulator,name=<device>' build
xcodebuild test -scheme WellSpent -destination 'platform=iOS Simulator,name=<device>' -only-testing:WellSpentTests
xcodebuild test -scheme WellSpent -destination 'platform=iOS Simulator,name=<device>' -only-testing:WellSpentUITests   # needs a live WellSpent-backend + UITEST_EMAIL/UITEST_PASSWORD env vars for the login/logout flows
```

If `xcodebuild`/`xcrun` says "requires Xcode" (Command Line Tools active instead of full Xcode), prefix commands with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`.

## Setup (new contributor)

1. Install `buf`: `brew install bufbuild/buf/buf`
2. `cd WellSpent-iOS && make generate` — confirms BSR auth works for Swift codegen before anything else
3. Open `WellSpent.xcodeproj` in Xcode
4. `cd ../../WellSpent-backend && make secrets-decrypt ENV=dev && make run` — API on `http://localhost:8080`; no CORS setup needed, CORS doesn't apply to native `URLSession`
5. Run the app in Simulator (⌘R) — Debug builds point at `http://localhost:8080` by default (`INFOPLIST_KEY_APIBaseURL` in the Debug build configuration)

## Architecture

- **UI**: SwiftUI only (no UIKit). Min deployment target iOS 17.0. **iOS/iPadOS only** — `SUPPORTED_PLATFORMS` is explicitly `"iphoneos iphonesimulator"` and `TARGETED_DEVICE_FAMILY` is `"1,2"` on every build configuration. Xcode's "App" template defaults new projects to multiplatform (adds `macosx`/`xros` + a "My Mac"/Vision Pro destination) unless you restrict destinations at creation time — this bit us once already (`.keyboardType(_:)`/`UIKeyboardType` are UIKit-only and don't exist on macOS, so a macOS destination fails to compile with a confusing type-checker error). If Xcode ever re-offers a "My Mac" or Vision Pro destination in the scheme picker, the project settings have regressed — re-check `SUPPORTED_PLATFORMS`/`TARGETED_DEVICE_FAMILY` in `project.pbxproj` before debugging anything else.
- **State/architecture**: MVVM using the `@Observable` macro (Observation framework) — no third-party state management. Every screen has a paired `View` + `@Observable @MainActor ViewModel`, dependencies (the ConnectRPC client) injected via `init`, not read from environment inside the view model.
- **Networking**: [connect-swift](https://github.com/connectrpc/connect-swift) — the Swift member of the same ConnectRPC family as `connect-es` (web) and `connect-go` (backend). Generated via `buf generate` against `buf.build/bewellspent/wellspent`, same BSR module the other two repos consume.
- **Concurrency**: Swift Concurrency (async/await) throughout. Note: `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` is set project-wide (Xcode's new default for SwiftUI apps) — plain logic types with no UI affinity (`JWT`, `KeychainTokenStore`, `FeatureFlags`, `APIEnvironment`) are explicitly marked `nonisolated` so they stay callable from background contexts (e.g. the networking layer's `TokenProvider` closure, which connect-swift may invoke off the main thread).
- **Local storage**: **Keychain** for the JWT only (`Auth/KeychainTokenStore.swift`, raw `Security` framework, no third-party wrapper). No local database — the app is a thin client, same as the web app; everything else is fetched live. `UserDefaults`/`@AppStorage` for simple prefs once Phase 2 needs them (locale/currency/view-mode) — not present yet, since nothing in the Auth-only phase consumes them (would be dead code).
- **Testing**: **Swift Testing** (`@Test`/`#expect`) for unit tests, **XCUITest** for a handful of end-to-end smoke flows only.

## Proto codegen (`Packages/WellSpentAPI`)

Generated Swift code is **not committed** (gitignored, same policy as `src/gen/` in web and `gen/` in backend) — always run `make generate` after cloning or after any proto change.

`Packages/WellSpentAPI` is a **local Swift package**, not files dropped directly into the app target. Reason: Xcode's `.xcodeproj` tracks file references explicitly for anything outside a synchronized folder group; regenerating proto code that adds/removes files would otherwise mean manually re-adding references in Xcode every time. A local SPM package auto-globs its `Sources/` folder instead — regeneration is just "the file tree changed," no project-file surgery. The app target depends on `WellSpentAPI` (plus its transitive `Connect`/`SwiftProtobuf` products, added as **direct** package dependencies too — see note below) and never touches `Gen/` directly.

- `buf.gen.yaml` (repo root) — Swift plugins `buf.build/apple/swift` + `buf.build/connectrpc/swift`, output to `Packages/WellSpentAPI/Sources/WellSpentAPI/Gen/`
- `Packages/WellSpentAPI/Sources/WellSpentAPI/Client/` — hand-written wrapper (not generated, not gitignored):
  - `APIClient.swift` — `makePublicClient(baseURL:)` / `makeAuthenticatedClient(baseURL:tokenProvider:onUnauthenticated:)`, mirrors web's `publicTransport` / `createTransport(token)` in `src/lib/api/client.ts`
  - `AuthInterceptor.swift` — attaches `Authorization: Bearer <token>`, reports 401s back to the app
  - `TokenProvider.swift` — `TokenProvider`/`UnauthenticatedHandler` typealiases (plain `@Sendable` closures, not a protocol — lets the token source be a synchronous Keychain read with no actor-isolation ceremony)
  - `@_exported import Connect` / `@_exported import SwiftProtobuf` in `APIClient.swift` — lets app code reference `ProtocolClient`, `ConnectError`, `Code`, and the `.with { }` builder pattern via just `import WellSpentAPI`

**Why the app target also directly links `Connect` and `SwiftProtobuf`, not just `WellSpentAPI`:** `@_exported import` re-exports symbols at the *source* level, but under Xcode's newer "debug dylib" test-hosting mechanism, that alone isn't enough for the *linker* to find them — you'll see `Undefined symbols` for `Connect.ProtocolClient` etc. at `xcodebuild test` time even though a plain `xcodebuild build` succeeds. Fix: both `WellSpent` and `WellSpentTests` targets have explicit direct package product dependencies on `Connect` and `SwiftProtobuf` (same resolved versions `WellSpentAPI` already pulls in), not just on `WellSpentAPI`. If a future `swift test`/`xcodebuild build` (not `test`) starts failing with similar undefined-symbol errors after adding a new generated-type usage in a new file, this is almost certainly the same class of issue — check whether the target needs the same direct dependency.

## Auth flow (Phase 1)

- `WellSpentApp.swift` owns the single `SessionStore` (`@Observable`, `@MainActor`) for the app's lifetime, injected via `.environment(session)`.
- `SessionStore` mirrors web's `AuthContext` + `TransportProvider`: `publicClient` (unauthenticated, for Register/Login/ListCountries) and `authenticatedClient` (built fresh on login, torn down on logout).
- **No proactive token refresh.** `AuthService.RefreshToken` exists on the proto/backend and is available in the generated client, but nothing calls it — this matches the web app's actual current behavior (it has the same RPC available and also never calls it). On expiry, both platforms just force a re-login. If this ever changes, change it on both platforms together or note explicitly why they diverge.
- `RootView` switches between the login/register flow and `BudgetListView` based on `session.isAuthenticated`, and calls `session.refreshAuthenticationState()` on `scenePhase == .active` — the Swift equivalent of web's `visibilitychange` expiry check.
- Google OAuth: `GoogleAuthButton` is rendered disabled behind `FeatureFlags.googleAuthEnabled` (reads the `FEATURE_GOOGLE_AUTH` process environment variable, off by default) — matches web's posture exactly. Real `GetGoogleAuthURL`/`ExchangeGoogleCode` wiring (needs its own Google Cloud OAuth client + `ASWebAuthenticationSession`) is out of scope until a later phase.

## Settings (Phase 2C-3)

A `UserService`/`AuthService`-shaped feature, not `BudgetService` — lives outside `BudgetDetailView` entirely, reachable via a gear icon on `BudgetListView`'s toolbar (`.topBarLeading`) that pushes `WellSpent/Settings/SettingsView.swift` through a plain `NavigationLink`, same push mechanism already used for budget rows.

- **Single `Form`, no sub-tabs** — mirrors web's single-page `settings/page.tsx` (`ProfileSettings.tsx` + `AccountManagement.tsx` combined). Three sections: Profile, Change Password, Account Management.
- **`SettingsViewModel.load()`** fetches `GetMe` + `ListCountries` in parallel (the `ListCountries` fetch + `.filter(\.isEnabled)` is the exact same pattern as `RegisterViewModel.loadCountries()` — reused, not reinvented). `apply(_ user:)` (prefill) is internal, not private, specifically so it's unit-testable as a pure step without a live network call — `SettingsViewModelTests` calls it directly with a constructed `Wellspent_V1_User`.
- **US-gating mirrors web's actual shipped behavior, not the "more correct" mechanism**: `isUnitedStates` is a plain `countryCode == "US"` check, gating the state field, filing status picker, and tax payment frequency picker. The proto/backend do support a real per-country feature-flag mechanism (`Country.features: [CountryFeature{name, is_enabled}]`, seeded with `before_tax_income` for `US`), but web's `ProfileSettings.tsx` fetches `ListCountries` and discards `features` entirely, hardcoding the same string check — iOS mirrors that (a deliberate parity choice, not an oversight, since diverging would make iOS "more correct" than web but inconsistent with it).
- **State field is free-text** (`TextField("State (e.g. CA)", ...)`), not a 50-item dropdown — reuses the same lightweight pattern `RegisterView` already established for state, rather than introducing a new full US-states picker just for Settings.
- **Switching away from US clears the US-only fields before saving** — `saveProfile()` sends `stateCode: isUnitedStates ? stateCode : ""`, `filingStatus`/`taxPaymentFrequency: isUnitedStates ? ... : .unspecified` — confirmed live: switching a test account from `US` back to `AR` actually clears `state_code`/`filing_status`/`tax_payment_frequency` server-side, not just hides them in the UI.
- **`FilingStatusLabel.swift`/`TaxPaymentFrequencyLabel.swift`** (`Shared/`) — new label helpers following the existing `BudgetCycleLabel`/`PaymentTypeLabel`/`IncomeTypeLabel`/`RecurringTypeLabel` convention. Both are real proto enums (unlike the raw-int `transaction_type_id` pattern), so no manual wire-string mapping is needed — just `switch` on the generated Swift cases.
- **Change Password is new for iOS** — web has zero UI for `ChangePassword` despite the RPC being fully implemented and tested server-side (confirmed via research: zero callers anywhere in `WellSpent-web/src/`). Included here as a deliberate choice (confirmed with the user) since the backend fully supports it and product docs list "change their password" as a real capability; iOS is the first client to expose it. Error mapping (`SettingsViewModel.passwordChangeErrorMessage(for:)`) follows the same `ConnectError.code`-switch shape as `RegisterViewModel.errorMessage(for:)`; confirmed live that a wrong current password comes back as `invalid_argument` with a passthrough-able message.
- **Delete Account is a plain confirm `.alert`** — no typed confirmation phrase, no password re-entry, matching web's `AccountManagement.tsx` exactly. `DeleteMe` is a **soft delete** (`docs/features/delete-account.md`: sets `status = 'disabled'`, `active_until = NOW() + 30 days` server-side, confirmed live that a subsequent `Login` is blocked with a recovery-hint message) — the client's only job on success is `session.endSession()`; `RootView` already switches to the login screen on `session.isAuthenticated` becoming false, so no manual navigation/dismiss is needed.
- **`BudgetListViewModel.replaceCurrentUser(_:)`** (mirrors the existing `replaceProfile(_:)`) lets `SettingsView`'s successful `saveProfile()` update `currentUser` in place — `BudgetListViewModel.currencyCode`/`localeIdentifier` (piped into every budget screen) reflect a currency/language change immediately, no `GetMe` refetch needed.
- **Deliberately deferred, not silently dropped**: the Subscription section (plan chip, disabled "Upgrade to Pro" — belongs to the tiered-subscriptions feature, not profile/account settings); the Plaid "Connected bank accounts" section (Phase 4).

## Budget flow (Phase 2A / 2B-1 / 2B-2 / 2B-3 / 2C-1 / 2C-2)

Phase 2A covers budgets/periods, people, and income sources; 2B-1 adds categories and payment methods; 2B-2 adds Variable transactions; 2B-3 adds Fixed expenses + mark-paid/unmark; 2C-1 adds expense allocations (Plan tab); 2C-2 adds the Expense Overview tab (actual vs. planned). Profile/account settings (2C-3) is a separate, non-budget-scoped feature — see "Settings" above.

- `WellSpent/Budget/` is the feature folder, one subfolder per management panel (`budgetList/`, `peoplePanel/`, `incomePanel/`, `categoriesPanel/`, `paymentMethodsPanel/`, `transactionsPanel/`), same layout convention as web's `src/components/budget/`.
- **`BudgetListView`/`BudgetListViewModel`** replaced the Phase 1 `HomePlaceholderView`/`HomeViewModel` stand-in — it now owns the top-level `GetMe` call (verify-email banner + the user's `currency`/`language`, used for money formatting everywhere downstream) alongside `ListBudgetProfiles`.
- **`BudgetDetailView` is the adaptive navigation shell**, not content itself: a bottom `TabView` on iPhone (`.compact` horizontal size class), a `NavigationSplitView` sidebar on iPad (`.regular`) — `BudgetSection` (`.plan`/`.transactions`/`.manage`) drives both. This collapses web's two independent nav layers (top tabs for Plan/Transactions/... *and* a separate sidebar/drawer for Income/Savings/Payment Methods/Categories/People) into one, since iOS has no good native equivalent of a persistent second nav rail alongside a tab bar. All three tabs are now real: Plan (allocations, 2C-1), Transactions (Variable + Fixed, 2B-2/2B-3), Manage (People/Income/Categories/Payment Methods). Default selected tab is `.manage` — revisit once Expense Overview (2C-2) adds a second view inside Plan.
- **`BudgetDetailViewModel.loadPeriod()` runs at the shell level** (`BudgetDetailView`'s own `.task`), not inside `BudgetManageView` — the Transactions tab needs `currentPeriod.id` for `budget_period_id` regardless of which tab loads first, so period loading can't be gated behind the user happening to open Manage. The Transactions tab shows a `ProgressView` until `currentPeriod` resolves.
- **Delete-budget dismiss must be passed down explicitly, not re-declared.** `BudgetManageView` sits inside its own nested `NavigationStack` (so People/Income/Categories/Payment Methods pushes don't disturb the other tabs), so a locally-declared `@Environment(\.dismiss)` there would only pop within that inner stack. `BudgetDetailView` captures `@Environment(\.dismiss)` at its own outer scope and passes a combined `dismissParent` closure (notify the parent list + actually dismiss) down instead. Apply the same pattern to any future tab/section that needs to close the whole budget detail screen from inside its own nested stack.
- **Removing a person** only prompts for a replacement when they have income sources attributed to them (`PersonReplacement.needsReplacement(person:incomeSources:)`, kept as a pure function in its own file so it's unit-testable without SwiftUI). **Known gap**: this doesn't check payment-method attribution even though Payment Methods now exist (2B-1) — `RemoveBudgetPersonRequest.replacement_payment_method_id` is never populated by the iOS client. Soft-delete (`is_active = false`, not a hard row delete) likely means this isn't a hard backend requirement the way `DeleteCategory`/`DeletePaymentMethod`'s replacement *is* (confirmed required via a live 401/`invalid_argument` check during 2B-1), but it should be verified and closed before Phase 3 touches People again.
- **Categories and payment methods always require a replacement on delete** — confirmed directly against the live backend during 2B-1 (`DeleteCategory`/`DeletePaymentMethod` both reject `replacement_id`/`replacement_payment_method_id` left at their zero value), unlike removing a person, where it's conditional. So `DeleteCategoryView`/`DeletePaymentMethodView` always show the picker; no `needsReplacement`-style check for either.
- **Payment type is immutable after creation** — `AddPaymentMethodView` has a type picker, `EditPaymentMethodView` doesn't (name/alias/color only), same Create/Edit-view split used for budgets in 2A (fields genuinely differ between add and edit, so two view models rather than one shared `Mode` enum like income/categories).
- **`transaction_type_id`/`transaction_frequency_id` are raw `int32` FKs on the wire, not proto enums** — their values (Fixed=1/Variable=2; One-off=1/Weekly=2/Bi-weekly=3/Monthly=4/Yearly=5) come from the seed order in `WellSpent-backend/internal/db/migrations/000001_init_schema.sql`, not a generated type, so every place that uses them has a doc comment pointing back at that migration. `TransactionsViewModel.variableTypeID`/`FixedExpensesViewModel.fixedTypeID` are the one source of truth for `2`/`1`; `AddEditTransactionViewModel` derives `transactionFrequencyID` from the `recurring` toggle (`recurring ? 4 : 1`).
- **Spent/Received sign convention** (`docs/features/negative-positive-transactions.md`): the Variable add/edit form always takes a positive amount plus a segmented Spent/Received toggle; `amount >= 0` = Spent (red, `-` prefix), `amount < 0` = Received (green, `+` prefix). `TransactionAmountFormatting.swift` holds this as pure, unit-tested logic — never inline a sign check in a view. **Fixed expenses have no sign flip** — always a plain expense, shown via `MoneyFormatting.format` directly, not `TransactionAmountFormatting`.
- **Not yet ported from web, deliberately deferred**: "Spent only"/"Excluded only" filters; Fixed expense payment-plan fields (`end_date`/`total_payments` — web's bidirectional end-date↔total-payments computation); "not due yet" placeholder rows for active Fixed templates whose anchor date hasn't spawned a transaction yet this period (lower priority since `CreateFixedExpenseResponse` returns the spawned transaction directly, so a freshly created template shows up immediately in the common case). All real web behaviors, just not in scope yet — note before assuming totals/filtering/Fixed-expense scheduling fully match web. (The Income-category-always-excluded rule *is* now implemented — see 2C-2 below — `TransactionsViewModel`/`FixedExpensesViewModel`'s `totalText` both use it.)
- **Fixed expenses live inside the Transactions tab, not a separate Manage panel** — mirrors web exactly (`TransactionsPanel.tsx`'s Fixed/Variable toggle over spawned `Transaction` rows, not a standalone `FixedExpense` list). `TransactionsListView` owns a `TransactionKind` (`.variable`/`.fixed`) segmented picker; `FixedExpensesListView` is the Fixed sub-view, each with their own toolbar "+" that only contributes to the shared toolbar while its content is actually on screen (SwiftUI composes `.toolbar` modifiers from whichever descendant view is currently in the hierarchy — no manual coordination needed since only one of the two is ever mounted at a time).
- **Mark-paid amount propagation to the template is entirely backend-side** — confirmed live: marking a Fixed transaction paid with an amount different from `planned_amount` automatically updates the linked `FixedExpense.planned_amount`. The iOS client only calls `MarkTransactionAsPaid`; never a follow-up `UpdateFixedExpense`.
- **`anchor_date` is the only date the user picks**; `day_of_month`/`day_of_week` are always derived from it client-side (`FixedExpenseScheduling.swift`, pure + unit-tested) since `CreateFixedExpenseRequest`/`UpdateFixedExpenseRequest` carry them as plain non-optional `int32` fields with no "compute this yourself" flag.
- **Established `Shared/` label/formatting/input helpers** (all `nonisolated enum`s with static functions, mirroring web's small formatting hooks): `Money+Formatting.swift`, `MoneyInput.swift` (shared `parseAmount`/`formatForEditing` for every money text field), `TransactionAmountFormatting.swift` (home to `sum(_:)`, the shared nanos-carry addition logic — handles both overflow and underflow for negative amounts — used by `totalDisplayText` and by `ExpensePlanCalculations`), `FixedExpenseScheduling.swift`, `ExpensePlanCalculations.swift` (2C-1, below), `BudgetCycleLabel.swift`, `BudgetRoleLabel.swift`, `IncomeTypeLabel.swift`, `RecurringTypeLabel.swift`, `PaymentTypeLabel.swift`. Reuse this pattern for any new proto enum/parsing/derivation rule rather than inlining in a view or view model. `PresetColors.all` (a fixed hex list, no `Color(hex:)` parser) is the shared source for every optional-color picker (people, categories, payment methods).
- **Expense Plan tab (2C-1)** — `WellSpent/Budget/expensePlanPanel/`. A per-category planned total (`ExpensePlanCalculations.plannedTotal`, pure + unit-tested, mirrors web's `getCatPlanned`): the Savings system category always sums `SavingsSource.amount`; every other category sums `ExpenseAllocation.plannedAmount` across people, and only if that's zero falls back to the sum of the current period's Fixed-type transactions' `plannedAmount` for that category — allocations and the fixed fallback are never combined. Categories are visible only if that total is nonzero; visible ones sort by planned total descending (`docs/features/category-list-order.md`). **Exclusion (`is_excluded`) does not affect planned totals** — confirmed live (excluding a Fixed transaction still counts its `plannedAmount` toward the category total); it only matters for Overview's actuals (2C-2, not built yet).
- **Allocation edits are per (category, person), not a single form** — tapping a category row opens `AllocateCategoryView`, one amount field per `BudgetPerson`, prefilled from any existing allocation. `AllocateCategoryViewModel.computeChanges` is the pure diff: a nonblank valid amount becomes an `UpsertExpenseAllocation` call, a row that had an allocation but is now blank becomes `DeleteExpenseAllocation`, and a row that was already blank stays untouched (no allocation created for a still-zero row). Same "sheet computes the value, parent view model performs the network call" split as `MarkAsPaidView` — `ExpensePlanViewModel.applyAllocationChanges` is the one that actually calls the RPCs and reloads.
- **Adding a not-yet-visible category to the plan** uses a flat picker sheet (`AddPlanCategoryView`, categories minus already-visible ones) rather than nesting a second sheet inside a sheet — selecting one dismisses the picker and `ExpensePlanView` presents `AllocateCategoryView` for it next. This replaces web's free-text "pin a category" Autocomplete with a simpler native equivalent.
- **"not due yet" fixed-expense placeholder rows are still deferred** in the Plan/Overview planned total (same deferral as 2B-3, carried over — only current-period Fixed transactions count toward the fallback, not templates that haven't spawned one yet).
- **Expense Overview tab (2C-2)** — `ExpensePlanView` gained a `Picker("View", selection:)` (`PlanKind`: `.plan`/`.overview`, local to the file, same pattern as `TransactionKind` in `TransactionsListView.swift`) switching between the existing Plan content and new `ExpenseOverviewListView`; toolbar "+" only shows for `.plan`. `ExpenseOverviewCalculations.swift` (`Shared/`) is the pure actual-spend helper, mirroring web's `computeActualTotals`: excludes manually-excluded and Income-category transactions (`isTransactionExcluded`), skips unpaid Fixed-type transactions from actual spend, attributes each transaction to a person via `paymentMethodID` → `PaymentMethod.budgetPersonID` (0 = unattributed), and buckets categoryless transactions into a separate "Uncategorized" total rather than dropping them. `ExpenseOverviewViewModel` reuses `ExpensePlanCalculations.plannedTotal` unchanged for the category-level planned figure, and calls it a second time with person-filtered inputs (allocations/savings/fixed amounts filtered to one person) to get the per-person planned figure — no new planned-total logic needed. Category sort/visibility reuses `ExpensePlanCalculations.sortedVisibleCategories`, just keyed on actual instead of planned, per `docs/features/category-list-order.md`.
- **Income total in Overview comes from `ListIncomeEntries` (period-scoped), not `ListIncomeSources`** (which Plan uses) — Overview is about what actually happened this period. **Known lifecycle gotcha, confirmed live**: adding an `IncomeSource` after a period already exists does not retroactively create an `IncomeEntry` for that period — there's no `AddIncomeEntry` RPC; entries only spawn when a new period is generated (same pattern as Fixed expenses spawning transactions). A test budget set up income-source-after-period will show $0 income in Overview; this is expected backend behavior, not an iOS bug.
- **Overspend is `planned > 0 && actual > planned`**, at both category and person level (`ExpenseOverviewCalculations.isOver`), shown as a red `+$X` chip. First use of `DisclosureGroup` (category → per-person breakdown) and Swift Charts (`import Charts`, a single `BarMark` per visible category, red when over budget else `.accentColor`) in this codebase — no third-party charting dependency, native framework only. Category/person colors are intentionally **not** parsed into `Color` for the chart (matches the existing "no `Color(hex:)` parser needed" simplification in `PresetColors.swift` — colors are picker-selection state only, never rendered as swatches elsewhere in the app either).
- **Deliberately deferred from web's Overview**: the per-category expanded transaction list (`CategoryTransactionList` — the Transactions tab already covers browsing all transactions); the percent-of-total chip (found in web's code but not in `docs/features/expense-overviews.md`'s business rules).
- **Every `View` struct has a `#Preview`** — a running requirement now, not just a 2B-3 cleanup. Views that hit the network use a throwaway unreachable `ProtocolClient` (`http://localhost:1`, same as the test suite's convention) so the canvas still renders the loading/empty state; views driven directly by init params (edit forms, delete/mark-paid pickers) get realistic sample proto values so they render fully populated.
- App version is now surfaced (`BudgetListView`'s footer, reading `Bundle.main.infoDictionary["CFBundleShortVersionString"]`) — bump `MARKETING_VERSION` in the **`WellSpent` app target's** Debug/Release configs only (not the test targets) per the existing patch/minor/major convention, now that there's a UI surface for it. (2B-1/2B-2 shipped without a bump — an oversight caught and corrected in 2B-3; don't repeat it.)
- **Every budget sub-screen's `.task` must always call `load()`, not just on first appearance (fixed post-2C-2, v1.4.1)** — the original pattern across every panel (`ExpensePlanView`, `ExpenseOverviewListView`, `TransactionsListView`, `FixedExpensesListView`, `IncomeListView`, `PeopleListView`, `CategoriesListView`, `PaymentMethodsListView`) was `guard viewModel == nil else { return }` before creating + loading — meaning once a tab had been visited once, switching away and back (TabView on iPhone, or the `NavigationSplitView` detail switch on iPad) never refetched, so edits made on one tab wouldn't show up on another without leaving `BudgetDetailView` entirely and pushing back in. Fixed by changing every one of these to `if viewModel == nil { viewModel = X(...) }` followed by an unconditional `await viewModel?.load()` — creates the instance once, but reloads its data on every reappearance. Every one of these views also now has `.refreshable { await viewModel?.load() }` for manual pull-to-refresh. **When a screen has a nested picker over two sub-views that each own a List** (`TransactionsListView`'s Variable/Fixed, `ExpensePlanView`'s Plan/Overview), both the outer view *and* the inner sub-view need their own `.refreshable` — SwiftUI's `.refreshable` is environment-based and would otherwise apply the *outer* view model's reload to the *inner* view's List. `BudgetListView` (the budgets list, outside `BudgetDetailView`) was left as-is — it already had `.refreshable` and handles staleness via explicit array-mutation callbacks (`addCreatedProfile`/`dismissAfterDelete`) rather than a full reload, which is a deliberate, different pattern for that screen.
- **`Transaction.amount` equals `plannedAmount` until a Fixed transaction is actually marked paid** — a bug in `FixedExpensesViewModel.totalText` (fixed v1.5.1, caught after pulling a matching fix from `WellSpent-web`'s `TransactionsPanel.tsx` grand-total) summed `.amount` without gating on `isPaid`, so unpaid Fixed expenses' planned amounts were counted as already spent in the Fixed tab's own Total row. `ExpenseOverviewCalculations.computeActualTotals` already gated on `isPaid` correctly (built that way from 2C-2) — only this one flat total had the gap. When summing Fixed-type transaction amounts anywhere, always filter `isPaid` first; this is exactly why `ExpenseOverviewCalculations`'s helper exists — prefer reusing/calling into it over re-deriving the same two-condition filter inline.
- **Savings (Phase 3A)** — `WellSpent/Budget/savingsPanel/`, reachable via a `NavigationLink("Savings")` in `BudgetManageView` (right after Income). Confirmed directly against web (`SavingsPanel.tsx`/`AddSavingsDialog.tsx`/`EditSavingsModal.tsx`) and the backend's compiled proto, not a research subagent — the one launched for this phase was lost mid-run (task tracker had no record of it when checked; if a background agent's result doesn't arrive, verify with `TaskOutput`/`block: false` before assuming it's still running, and just redo the research directly rather than waiting indefinitely).
  - **`payment_days` is a set of specific calendar days the user taps directly** — not a single reference day the client replicates into a pattern. `SavingsPaymentDaysGrid.swift` (shared by Add/Edit) renders every day `1...daysInMonth` as a toggle; the *count* selected is the frequency (1→Monthly, 2→Bi-weekly, 4→Weekly — `SavingsFrequencyLabel.swift`, purely a display derivation, there is no frequency picker anywhere).
  - **`budget_person_id` is never sent by the client** — confirmed against the backend's compiled Go proto (`WellSpent-backend/gen/wellspent/v1/budget.pb.go`), which has no such field on either request message. It's inferred entirely from the chosen payment method's own attribution; confirmed live that clearing the payment method on `UpdateSavingsSource` also clears `budget_person_id` on the response.
  - **Add requires exactly 1/2/4 days and a payment method; Edit allows 0-4 days and an optional ("None") payment method** — two different validation sets (`AddSavingsSourceViewModel`/`EditSavingsSourceViewModel`), matching web's differing `VALID_COUNTS` between its Add and Edit dialogs.
  - **The tax-reserve row (`is_tax_reserve = true`) is fully system-managed, never client-triggered** — recalculated server-side (`recalculateTaxReserve` in the backend's `budget_profile_service.go`) after income/tax-setting changes; confirmed live that adding a before-tax US income source auto-creates it with `federal_amount`/`state_amount` populated. `SavingsListView` renders it with no edit/delete affordance at all (a plain `info.circle` + breakdown instead of the swipe-to-delete/tap-to-edit every other row gets) — this is a display-only distinction, there's no RPC-level block to route around.
  - **Delete is plain** — `DeleteSavingsSourceRequest{id, budget_profile_id}`, no replacement picker, unlike categories/payment methods.
- **Invites (Phase 3B)** — `WellSpent/Budget/invitePanel/` (send/list/cancel/resend, reachable via `NavigationLink("Invitations")` in `BudgetManageView`, right after Payment Methods) + `WellSpent/Invites/` (the Accept screen, not budget-scoped). Role display/change (`BudgetRoleLabel.swift`, `PeopleViewModel.updateRole`) turned out to already exist from an earlier phase — `BudgetPerson.role` was in the proto from day one and got handled generically alongside People, so the only net-new work here was `InviteService` + native acceptance.
  - **Native invite acceptance via Universal Links, not a browser redirect** — the invite email always links to `{FRONTEND_URL}/en/invite/{token}` (hardcoded locale, backend's `invite_service.go`). `WellSpent-web/public/.well-known/apple-app-site-association` (added alongside this phase, its own PR) lets Apple verify the iOS app's Associated Domains entitlement (`WellSpent/WellSpent.entitlements`, `applinks:bewellspent.com`) against team `76FQ7V92H5` / bundle `com.bewellspent.WellSpent`. `RootView.swift` owns `.onOpenURL` (parses the token via `InviteDeepLink.token(from:)`, `Shared/`) and presents `AcceptInviteView` as a `.fullScreenCover` whenever authenticated; `BudgetListView` gets `.id(budgetListRefreshTrigger)` so a successful accept forces a full remount (and thus a fresh `ListBudgetProfiles`) rather than relying on its deliberately-guard-once `.task`.
  - **A previously-assumed blocker turned out to be stale docs** — `docs/specs/01-requirements.md` says web deployment is "Planned," and the backend's example env defaults `FRONTEND_URL` to `localhost:3000`. The user corrected this: `https://bewellspent.com` is live on Vercel today. Confirmed via `sops --decrypt` on `WellSpent-backend/.env.prod.enc` (needs `SOPS_AGE_KEY_FILE=~/.config/sops/age/keys.txt` — the default macOS key path SOPS looks for isn't where this machine's key actually lives) and a follow-up `curl`. Don't take a "planned"/"pending" doc note as proof a thing doesn't exist yet — ask if a stated blocker seems surprising before designing around it.
  - **`isAdmin` reuses `SessionStore.userID`** (already resolved from the JWT `sub` claim for other purposes) compared against the budget owner and the matching `BudgetPerson.role` — no new JWT-decoding code needed, mirrors web's `useBudgetRole`.
  - **Simulator can't fully verify Universal Link routing** — `xcrun simctl openurl` delivers the URL and `onOpenURL` fires without crashing, but real domain-association verification (Apple's swcatalog) isn't reliably exercised outside a real device/TestFlight build. Treat a Simulator no-op on the visual accept screen as expected, not a regression, as long as unit tests for `InviteDeepLink` pass and the AASA file is confirmed live.
  - **Deliberately deferred**: full read-only UI enforcement (hiding every mutation affordance for Viewers/non-collaborators across every panel — the backend already enforces roles server-side regardless of client UI); the unauthenticated accept-then-redirect-through-login flow (web's `?redirect=` pattern) — the Accept cover here only ever shows once already authenticated.
- **Alerts (Phase 3C, Part 1)** — `WellSpent/Budget/alertsPanel/` (Alert Settings, `NavigationLink` in `BudgetManageView` right after Invitations) + `WellSpent/Notifications/` (bell + popover inbox, not budget-scoped internally but shown per-budget).
  - **`alert_type`/`channel`/`threshold_scope` are plain strings on the wire, not proto enums** (unlike `BudgetRole`/`InviteStatus`) — same "raw wire constant" pattern as `transaction_type_id`, wrapped in real Swift enums (`AlertType.swift`/`AlertChannel.swift`/`ThresholdScope.swift`, `Shared/`).
  - **Free-tier gating is client-side UX polish, not the enforcement boundary** — confirmed live that the backend independently blocks a 3rd subscription (`"free tier: budget alerts are limited to 2..."`) and `new_transaction` for Free (`"...require a Pro subscription"`) regardless of what the client shows. `AlertsViewModel.isAtLimit`/`visibleAlertTypes` just avoid presenting a guaranteed-fail toggle; updating an already-enabled type's channel/threshold is always allowed even at the limit.
  - **The notification bell is shared across all three `BudgetDetailView` tabs from one `NotificationBellViewModel` instance**, not three independent ones — created once and polled (30s `Task.sleep` loop) from `BudgetDetailView`'s own `.task` (alongside the existing period-loading task). Switching tabs never restarts the poll loop or re-fetches, since it's the same `@State` instance throughout the screen's lifetime. **Where it's rendered from was wrong initially and had to be corrected — see the "nested-NavigationStack chrome bug" note below; the bell (and every other toolbar button/title on this screen) now lives in `BudgetDetailView`'s own single outer `.toolbar`, not inside `planContent`/`transactionsContent`/`manageContent`.**
  - **No real-time delivery yet** — this matches web exactly (`refetchInterval: 30_000`, no streaming RPC exists in `notification.proto`). Real push notifications (APNs) are Part 2 of this phase, tracked separately since they need a real Apple Developer Program APNs key the user has to generate — not something to build silently into "alerts" as if it were free.
  - **`.popover` needs `.presentationCompactAdaptation(.popover)`** on the presented content (not the `.popover` modifier itself) to float on iPhone the way it does on iPad by default — otherwise it adapts to a full sheet-like presentation on compact width.
- **Push notifications (Phase 3C, Part 2)** — real APNs push, riding along with every in-app notification (not a separate `channel` value — see the backend's `sendInApp`→`sendPush` call in `notification_service.go`). Cross-repo: `WellSpent-proto`'s `RegisterDeviceToken` RPC → `WellSpent-backend`'s `device_token` table + `apns2`-based send (token-auth `.p8`, not certificate-based) → this repo's registration.
  - **`UIApplicationDelegateAdaptor` is required** — SwiftUI's `App` protocol has no callback for the raw APNs device token. `AppDelegate.swift` (sibling of `WellSpentApp.swift`, not nested in `App/` — that folder happens to hold only `RootView.swift`) exposes `static var onDeviceToken`/`onRegistrationError` closures rather than instance properties, set once in `WellSpentApp.init()` (before `@UIApplicationDelegateAdaptor` hands back an instance to hold a reference to) with a `[weak sessionStore]` capture.
  - **Permission request happens after login, not at cold launch** — `RootView.swift`'s `.task(id: session.isAuthenticated)` calls `PushNotificationRegistrar.requestPermissionIfNeeded()`, which requests only if `.notDetermined`, silently re-registers if already `.authorized` (harmless — `RegisterDeviceToken` upserts by token), and does nothing if previously `.denied` (iOS won't re-prompt anyway).
  - **Confirmed live in Simulator**: installing/launching the app while already logged in (from a prior session's Keychain token) triggered the real system permission dialog automatically, proving the whole chain through `requestAuthorization` works. Simulator *can* issue a synthetic device token when "Allow" is tapped (unlike Universal Link domain-verification, which Simulator can't do at all) — but automating that tap wasn't possible in this environment (no visible `Simulator.app` process for accessibility scripting), and real end-to-end APNs delivery needs a physical device regardless.
  - **`APNS_*` backend config is empty by default** (`sendPush` no-ops with a log warning, same posture as `RESEND_API_KEY`) — real send-verification is blocked on the user providing a real Apple Developer Program Auth Key. This is the second time in this project a stated blocker turned out differently than assumed going in (see the Invites/Universal-Links note above about the "planned" deployment doc) — the difference here is the blocker is real and still open, not a stale doc; don't attempt to fabricate or guess at credentials, and don't claim push "works" until a real key is in place and tested on a real device.
  - **`aps-environment` doesn't need a Debug/Release split** — a single `development` value in `WellSpent.entitlements` is standard; Xcode's distribution/archive signing automatically upgrades it to `production` for App Store builds via the distribution provisioning profile, it's not something to toggle per build configuration manually.
- **`BudgetDetailView`'s nested-NavigationStack chrome bug (fixed v1.9.1)** — `BudgetDetailView` is pushed via `NavigationLink` onto `BudgetListView`'s `NavigationStack`, but also nests its own per-tab `NavigationStack`s inside its `TabView`/`NavigationSplitView` (`tabView`/`splitView`) so each tab keeps independent push state. That double nesting means the *outer* stack — the one `BudgetListView` actually pushed onto — owns the only nav bar that's really rendered:
  - `.toolbar` items declared on an inner nested stack surface only **transiently**, during the push-in animation, then get dropped once the outer stack's bar takes over — this is what made the notification bell (and even the pre-existing "+"/"…" buttons) appear to vanish, or flash for a moment then disappear, depending on exactly where the duplicate `.toolbar` lived.
  - `.navigationTitle` from an inner nested stack **never** surfaces at all in the outer bar (unlike toolbar content, it doesn't bubble up), which is why `ExpensePlanView`/`TransactionsListView`/`BudgetManageView`'s own `.navigationTitle` calls looked correct in their standalone `#Preview` (single-level `NavigationStack`, no nesting) but never actually showed in the real, doubly-nested app.
  - The fix: **all title and toolbar chrome for this screen now lives on `BudgetDetailView` itself** (the outer, persistently-rendered level) — `currentTitle` and `toolbarContent` there switch on `selectedSection` (plus lifted sub-tab state, `planSelectedKind`/`transactionsSelectedKind`) to reproduce what each tab used to declare locally. The leaf views (`ExpensePlanView`, `TransactionsListView`, `FixedExpensesListView`, `BudgetManageView`) still keep their own `.navigationTitle`/`.toolbar` declarations purely for canvas-preview accuracy in isolation — those never render in the real app and must not be relied on there. Add/edit/delete triggers (`isAddCategoryPresented`, `isAddTransactionPresented`, `isAddFixedExpensePresented`, `isEditBudgetPresented`, `isDeleteBudgetConfirmationPresented`) are `@Binding`s owned by `BudgetDetailView` and passed down, so the buttons live in the outer toolbar while the actual `.sheet`/`.confirmationDialog` presentation stays local to each leaf view (presentation isn't affected by the nesting the way title/toolbar are).
  - **Any new tab-level toolbar button or title on this screen must be added to `BudgetDetailView`'s `currentTitle`/`toolbarContent`, never to the leaf view's own `.toolbar`/`.navigationTitle`** — the latter will build fine, look right in `#Preview`, and silently not work in the app.
  - **Separately, `TabView` keeps every tab mounted once created**, so a leaf view's `.task` (per the v1.4.1 reload fix above) only fires once on first mount and does not refire on simple tab reselection — switching Plan → Transactions → Plan showed stale data. Fixed by adding an `isActive` flag (`selectedSection == .plan` / `.transactions`, computed in `BudgetDetailView` and passed down) with `.onChange(of: isActive) { _, newValue in if newValue { Task { await viewModel?.load() } } }` in `ExpensePlanView`/`TransactionsListView`/`FixedExpensesListView`, so reselecting a tab reloads it the same way fully leaving and re-entering the budget already did.
  - Caught via user-reported visual bugs across several iterations (missing bell → flickering title → buttons flashing then disappearing → stale data on tab switch), not from build/test output — `xcodebuild build`/`test` and even the `BudgetSmokeTests` XCUITest suite (fast enough to tap a button during its brief transient-visible window) all passed throughout, underscoring that this class of bug is only catchable by actually looking at the running app.

- **Tiered subscriptions (Phase 4A)** — `WellSpent/Shared/PlanLabel.swift` (text/systemImage/tint for `AccountPlan` — a real proto enum, unlike `alert_type`'s raw wire strings, so this is a display helper only, not a wire-format shim); Subscription section in `SettingsView.swift`/`SettingsViewModel.swift` (plan badge + permanently-disabled "Upgrade to Pro" button, `SettingsViewModel.apply(_:)` now also copies `plan`); free-tier limit gating on `PeopleViewModel`/`IncomeViewModel` (max 2 people / 2 income sources); `WellSpent/Budget/reportsPanel/ReportsPlaceholderView.swift` (`NavigationLink` inside `BudgetManageView`, not a 5th bottom tab — see rationale below) + `WellSpent/Ads/` (AdMob banner + App Tracking Transparency).
  - **No shared place holds `plan`** (`SessionStore` only has auth/token state; `BudgetListViewModel.currentUser` isn't threaded into `BudgetDetailView`) — followed the *existing* precedent from `AlertsViewModel` (its own independent `GetMe` call, `private(set) var isFree`) rather than a wider plumbing refactor. `PeopleViewModel`/`IncomeViewModel` each now make their own `GetMe` call in `load()`. This means the budget screen fires 3 independent `GetMe` calls across its tabs (Alerts/People/Income) — a deliberate consistency choice, not maximally efficient. Both view models also gained `setStateForTesting(...)`, same test-seam pattern as `AlertsViewModel`.
  - **Income's free-tier limit is a conservative approximation, not exact** — the backend's real rule is 2 sources *per person*, but `IncomeListView`/`IncomeViewModel` are budget-wide (no per-person breakdown at that layer), so `IncomeViewModel.isAtLimit` gates on the budget's *total* source count instead. Documented inline; client-side gating is UX polish only regardless (confirmed live: the backend independently enforces the real per-person limit and returns `"free tier: income sources are limited to 2 per person..."` even if the client's approximation would have allowed the add).
  - **Reports lives inside `BudgetManageView` via `NavigationLink`, not a 5th `BudgetSection` tab** — web has it as a top-level tab, but it's placeholder-only content right now (matches web's own "coming soon" copy), and a 5th iPhone bottom tab for that would crowd an already-3-tab (4 after Phase 4B) bar for no functional benefit yet. Promoting it to a real `BudgetSection` case is a small, isolated change once Reports does something real — see the "nested-NavigationStack chrome bug" note above for exactly what that change needs to touch.
  - **First third-party (non-Apple/connectrpc) SPM dependency in this app**: `GoogleMobileAds` (`googleads/swift-package-manager-google-mobile-ads`, resolved to v11.13.0 via `upToNextMajorVersion` from 11.0.0). Added to the `WellSpent` app target only (not `WellSpentTests`/`WellSpentUITests` — nothing there references ad SDK symbols). No `NS_SWIFT_NAME` renaming or Swift-native overlay exists in this SDK version — confirmed directly against the resolved package's Objective-C headers (`GADBannerView`, `GADRequest`, `GADAdSizeBanner`, `GADMobileAds.sharedInstance()`), so `AdBannerView.swift` uses the classic `GAD`-prefixed names, not a newer unprefixed naming scheme some other Google SDKs use. `GADBannerView` is a `UIView`, wrapped via plain `UIViewRepresentable` (no view-controller-hosting needed).
  - **`GADMobileAds.sharedInstance().start(completionHandler: nil)` runs unconditionally at cold launch** (`WellSpentApp.init()`) — doesn't show UI or need ATT permission itself, safe before login. `TrackingPermission.swift` (mirrors `PushNotificationRegistrar.swift`'s shape) requests the separate ATT prompt after login instead (`RootView.swift`, alongside the existing push-permission `.task(id:)`), needed for personalized (vs. only non-personalized) ads.
  - **Real AdMob App ID, test banner ad unit ID** — the user has a real AdMob App ID (`ca-app-pub-8875066498176869~1740075152`, same publisher number as web's existing AdSense ID) already set in `GADApplicationIdentifier`. `AdBannerView.adUnitID` still defaults to Google's public test banner ad unit ID (`ca-app-pub-3940256099942544/2934735716`) since no real banner ad unit ID exists yet — swap it once one exists in the AdMob console. A real App ID + test ad unit ID is a standard, safe development configuration; the App ID alone being unset is what actually crashes the SDK (see below), not the ad unit ID.
  - **Transaction Review (Phase 4B)** — new `WellSpent/Budget/transactionReviewPanel/` (`TransactionReviewViewModel.swift`/`TransactionReviewListView.swift`), a 4th `BudgetSection.review` tab (Plan / Transactions / **Review** / Manage) with a pending-count `.badge(_:)` on the tab item, matching web's tab badge.
    - **`reviewViewModel` is owned by `BudgetDetailView`, not `TransactionReviewListView`** — same reasoning as `notificationViewModel`: the tab item's badge needs the pending count at the outer level, so the view model is created once in its own `.task` (alongside the period-loading and notification-bell tasks) and passed down, rather than each leaf view fetching its own copy.
    - **`ListTransactionReviews` doesn't filter by status server-side** — confirmed live (a dismissed review disappeared from the list entirely rather than showing `status: "dismissed"`, and a confirmed one stayed with `status: "confirmed"`) — `TransactionReviewViewModel.pendingReviews` client-filters to `status == "pending"`, mirroring web's `TransactionReviewPanel.tsx`.
    - **Confirm excludes, does not delete, the imported variable transaction** — confirmed live: after `ConfirmTransactionReview`, the variable transaction still appears in `ListTransactions` with `isExcluded: true`, and the matched Fixed-type transaction shows `isPaid: true`. `docs/features/transaction-review.md`'s "deletes the variable transaction" line is stale; the code (`transaction_service.go`) is the source of truth, not the spec doc.
    - **`MarkTransactionForReview` (manual "flag as duplicate") is real and working** (verified live — used it directly to generate a test review row, since no Plaid data exists on iOS yet), but deliberately has no UI here, matching web's own `TransactionReviewPanel.tsx`, which doesn't call it either.
    - **No proto/backend changes** — all 4 RPCs (`ListTransactionReviews`/`ConfirmTransactionReview`/`DismissTransactionReview`/`MarkTransactionForReview`) already existed and were fully wired server-side; this phase is iOS-client-only.
  - **`INFOPLIST_KEY_<Name>` does not reliably synthesize arbitrary custom keys in this project** (Xcode 26.6, `objectVersion = 77`) — discovered the hard way: `GADApplicationIdentifier` set via `INFOPLIST_KEY_GADApplicationIdentifier` never made it into the built `Info.plist` (confirmed via `PlistBuddy` after a full `xcodebuild clean`), causing a real crash (`GADInvalidInitializationException`) on every launch. Isolated by testing a throwaway arbitrary key (`INFOPLIST_KEY_MyRandomCustomKey123`) — also silently dropped — which then revealed the **pre-existing** `INFOPLIST_KEY_APIBaseURL` had *never* actually worked either; it was masked the entire time because `APIEnvironment.swift` falls back to `"http://localhost:8080"` when the key is missing, which happens to be correct for local dev. Apple's own "known" keys (e.g. `NSUserTrackingUsageDescription`, scene manifest, orientation flags) *do* synthesize correctly — only genuinely arbitrary/custom key names are affected. **Fix**: a real, physical `Info.plist` fragment (`WellSpent-iOS/Info.plist` — deliberately placed *outside* the `WellSpent/` `PBXFileSystemSynchronizedRootGroup`, since a copy placed inside it gets auto-swept into "Copy Bundle Resources" and collides with `INFOPLIST_FILE` processing) merged in via `INFOPLIST_FILE = Info.plist;` alongside `GENERATE_INFOPLIST_FILE = YES` — this is Apple's documented, supported combination for exactly this case. `APIBaseURL` now reads `$(API_BASE_URL)` (a real build setting, still overridable per Debug/Release) inside that fragment, with `-expandbuildsettings` doing the substitution same as before. **Any future arbitrary custom Info.plist key must go in `WellSpent-iOS/Info.plist`, not `INFOPLIST_KEY_*`** — the latter silently does nothing for non-Apple-known keys in this project.

## Testing

- **Swift Testing**, one `@Suite` per type, in `WellSpentTests/` (app target logic) and `Packages/WellSpentAPI/Tests/WellSpentAPITests/` (networking layer). Covers: JWT decode/expiry edge cases, Keychain round-trips (real Simulator Keychain — works fine under `xcodebuild test`, each test uses a unique service name to avoid cross-test interference), `AuthInterceptor` header injection and 401 handling (constructed `HTTPRequest`/`ResponseMessage` values directly, no network mocking needed), view model validation + error-code-to-message mapping, `SessionStore` state transitions.
- Deliberately **not** unit tested: SwiftUI view bodies (no assertion value without a UI-test harness) and thin one-line pass-throughs to generated RPC methods.
- **XCUITest** smoke flows in `WellSpentUITests/`: `LoginSmokeTests` (valid + wrong-password), `RegisterSmokeTests` (fresh timestamp-suffixed email — no seeded account needed), `LogoutSmokeTests` (log out, then relaunch *without* the reset flag to prove the Keychain token was actually cleared, not just in-memory state), `BudgetSmokeTests` (create a budget, add a person, add an income source, then delete the budget as cleanup so repeated runs don't leave orphan data on the real backend). Login/Logout/Budget tests need a seeded account: `export UITEST_EMAIL=... UITEST_PASSWORD=...` before running. All need `WellSpent-backend` running locally.
- The app supports a `-uiTestResetSession` launch argument (checked in `WellSpentApp.init`) that clears the Keychain token before `SessionStore` restores a session — every UI test starts from a known logged-out state instead of inheriting whatever the Simulator's Keychain has from a previous run.
- Accessibility identifiers are set explicitly on every interactive element and screen-level container (not inferred from label text) — i18n will change label text later, identifiers won't.

## Version bump

Wired up as of Phase 2A: `MARKETING_VERSION` in the **`WellSpent` app target's** Debug/Release build configs (`WellSpentTests`/`WellSpentUITests` configs are untouched — their version number has no user-facing meaning), read at runtime via `Bundle.main.infoDictionary["CFBundleShortVersionString"]` and shown in `BudgetListView`'s footer. Bump it for every feature going forward, same patch/minor/major convention as the other repos.

## Git workflow

Same convention as the other sub-repos: work happens on `develop`, never directly on `main`.

```bash
git checkout develop   # or: git checkout -b develop, if it doesn't exist yet locally
git add WellSpent/... Packages/WellSpentAPI/Package.swift Packages/WellSpentAPI/Sources/WellSpentAPI/Client/... Packages/WellSpentAPI/Tests/...
git commit -m "feat: meaningful description of what changed"
git push origin develop
```

(No PR/auto-merge automation set up on this repo yet — add `gh pr create --base main --head develop ... && gh pr merge develop --auto --merge` here once CI exists for this repo, matching the other three.)

## Generated files — do not edit

`Packages/WellSpentAPI/Sources/WellSpentAPI/Gen/` — generated by `make generate` from `buf.build/bewellspent/wellspent`. Gitignored.

## Roadmap

"Phase 2 — core budget loop" was too large for one confirmed slice (same reasoning as Phase 0+1), so it's split further:

- **Phase 2A (done)** — budgets/periods, people, income sources. See "Budget flow (Phase 2A / 2B-1)" above.
- **Phase 2B-1 (done)** — categories, payment methods. Same section above.
- **Phase 2B-2 (done)** — Variable transactions (create/edit/delete, Spent/Received sign flip, exclude toggle). Fills in the real Transactions tab. No filters ("Spent only"/"Excluded only") yet — see "Budget flow" above.
- **Phase 2B-3 (done)** — Fixed expense templates (`FixedExpense` CRUD) + mark-paid/unmark on their spawned transactions, via a Variable/Fixed picker inside the Transactions tab. Payment-plan fields (`end_date`/`total_payments`) and "not due yet" placeholder rows deliberately deferred — see "Budget flow" above.
- **Phase 2C-1 (done)** — expense allocations (`ExpenseAllocation` CRUD), filling in the real Plan tab: category list with planned totals, per-person allocate sheet, add-category picker, income/committed/remainder footer. See "Budget flow" above.
- **Phase 2C-2 (done)** — Expense Overview tab (actual vs. planned, chart, collapsible per-person actuals, overspend indicators), via a Plan/Overview picker inside the Plan tab. Also closed a gap from 2B-2: Transactions/Fixed totals now apply the Income-category-always-excluded rule too. See "Budget flow" above.
- **Phase 2C-3 (done)** — profile/account settings (profile fields, change password, delete account) — the User/Auth-service equivalent of web's Settings pages, not a Budget-service feature like the rest of 2C. See "Settings" above. **Phase 2C is now fully complete.**
- **Phase 3A (done)** — Savings (`SavingsSource` CRUD, tax-reserve read-only rendering). See "Savings (Phase 3A)" above.
- **Phase 3B (done)** — Invites (`InviteService` CRUD + native Universal Link acceptance). Role display/change turned out to already be shipped. See "Invites (Phase 3B)" above.
- **Phase 3C (done, real APNs send-verification pending)** — alerts/notifications. Part 1 (Alert Settings + notification bell, 30s polling) and Part 2 (push infrastructure: proto/backend/iOS all built and wired) are both shipped — see "Alerts (Phase 3C, Part 1)" and "Push notifications (Phase 3C, Part 2)" above. The one remaining step isn't code: the user needs to generate a real Apple Developer Program APNs Auth Key and add it to `.env.dev.enc`/`.env.prod.enc` before a real push can actually be sent. **Phase 3 (savings, invites, alerts) is now fully complete.**
- **Phase 4A (done)** — Tiered subscriptions (plan badge, free-tier limit gating, Reports placeholder + AdMob banner). See "Tiered subscriptions (Phase 4A)" above.
- **Phase 4B (done)** — Transaction Review (4th "Review" tab, badge-count, Confirm/Dismiss). See "Transaction Review (Phase 4B)" above.
- **Phase 4C (not started)** — Plaid.
- **Theme toggle (not scheduled)** — web's `docs/features/color-scheme.md` includes an explicit Light/Dark/System toggle persisted to `localStorage` (needed there since MUI doesn't follow the OS automatically); iOS was never given an equivalent phase. Lower priority than on web — SwiftUI already follows the OS's light/dark appearance for free with zero code — but an explicit override (a picker calling `.preferredColorScheme()`, persisted via `@AppStorage`) would still be needed for full parity with web's toggle. Entity colors (categories/payment methods/people), the other half of that spec, are already done (`PresetColors.swift`, since 2A/2B-1). Deliberately deferred, not forgotten — revisit if/when full web parity on personalization becomes a priority.

Each phase should update this file's Architecture/Testing sections as new patterns get established, the same way `WellSpent-web/CLAUDE.md` accumulated its "Component composition" and "Mobile + desktop support" sections over time.
